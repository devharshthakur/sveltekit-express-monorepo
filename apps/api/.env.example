# API configuration — copy this file to .env and fill in your values
PORT=8000

# CORS: comma-separated list of allowed origins (use `*` to allow all)
CORS_ORIGIN=http://localhost:5173,http://localhost:4173
